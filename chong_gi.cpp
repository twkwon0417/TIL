#include <bits/stdc++.h>
using namespace std;

int a, b, _a, _b, visited[100004][100004], ans = -1;
queue<pair<int, int>> q;


int main(){
    cin >> a >> b >> _a >> _b;
    
    q.push_back({0, 0});
    visited[0][0] = 1;
    while(q.size()){
        
    }
}
